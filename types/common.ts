export type BookLanguage = "zh" | "en";

export type GenerationStatus = "idle" | "generating" | "done" | "error";

export type ModelConfigType = "vision" | "tts";

export type ModelConfigProvider = "openai-compatible" | "xiaomi-mimo";
